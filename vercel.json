{
    "buildCommand": "pnpm prisma generate && pnpm build",
    "installCommand": "pnpm install",
    "builds": [
      {
        "src": "nuxt.config.ts",
        "use": "@nuxtjs/vercel-builder"
      }
    ]
  }